#!/bin/bash

arch=$(dpkg --print-architecture)

IAM=michaeldallen/m2c-python3-${arch}

# docker pull ${IAM} && \
docker \
    run \
	-it \
        --rm \
        --volume "`pwd`":/workdir:ro \
        ${IAM} \
        "$@"


#EOF
